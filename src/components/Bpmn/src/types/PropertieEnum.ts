/**
 * BPMN 属性enum
 *
 * @author zoulan
 * @since 2024-06-18
 */
import { BPMN_MODDLE_PREFIX } from '/@/settings/componentSetting';

export enum PropertieEnum {
  CANDIDATE_TYPE = `${BPMN_MODDLE_PREFIX}:assigneeType`
}
